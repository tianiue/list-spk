#!/bin/sh

# DSM 7.2中共享文件夹路径规范：/volume1/[文件夹名]
MOUNT_DIR="/volume1/OpenList_Mount"

# 创建挂载目录（需确保权限正确，允许用户访问）
mkdir -p "$MOUNT_DIR"
chmod 777 "$MOUNT_DIR"  # 可根据需求调整权限（如限制为管理员）

# 记录安装路径到配置文件（供客户端使用）
echo "mount_dir=$MOUNT_DIR" > /var/packages/OpenListClient/etc/config.ini

exit 0
